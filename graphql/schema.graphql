type Query {
    getAllProducts(filter: productFilter): [Product]
}
type Product {
    id: String
    product: String
    price: Float
}
type Mutation {
    createProduct(input: ProductInput): Product
}
input productFilter {
    categoryId: String
}
input ProductInput{
    
    product: String!
    price: Float!
}